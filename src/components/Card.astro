---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "border-border bg-bg-elevated hover:border-fg-dim flex flex-col gap-2 border p-4 transition-colors duration-200",
    className,
  ]}
>
  <slot />
</div>
